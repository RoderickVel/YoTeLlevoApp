import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yotellevo.app',
  appName: 'YoTeLlevoApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
